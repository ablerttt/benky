//client/src/components/ViewList/styles.js

const styles = (theme) => ({
  "@global": {
    body: {
      backgroundColor: "white",
    },
  },
  root: {
    flexGrow: 1,
  },
  paper: {
    padding: theme.spacing(2),
    textAlign: "center",
  },
});

export default styles;
